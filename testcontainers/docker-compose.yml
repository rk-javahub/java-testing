version: "3.9"

services:
  oracle:
    image: gvenzl/oracle-xe:21-slim
    container_name: oracle-xe
    ports:
      - "1521:1521"
    environment:
      ORACLE_PASSWORD: ADMIN
      APP_USER: rohit
      APP_USER_PASSWORD: rohit007
    volumes:
      - oracle-data:/opt/oracle/oradata

volumes:
  oracle-data:
